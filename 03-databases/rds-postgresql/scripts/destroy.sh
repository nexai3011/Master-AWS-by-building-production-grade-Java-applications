#!/usr/bin/env bash
set -euo pipefail

echo "Destroy database resources after completing the lab to avoid AWS charges."
echo "Use the AWS CLI or console to delete the RDS instance and related security resources."
